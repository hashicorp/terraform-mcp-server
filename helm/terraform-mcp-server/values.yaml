# Default values for terraform-mcp-server.

replicaCount: 1

image:
  repository: hashicorp/terraform-mcp-server
  pullPolicy: IfNotPresent
  # Defaults to the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  automount: true
  # Generated from the fullname template if empty and create is true.
  name: ""

podAnnotations: {}
podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

nodeSelector: {}
tolerations: []
affinity: {}

# Mount a cert and key here to serve TLS directly, then point
# mcpServer.tls.certFile and keyFile at the mount paths.
volumes: []
volumeMounts: []

# Maps to the server's environment variables.
mcpServer:
  # stateful or stateless. stateless is useful with multiple replicas behind a
  # load balancer that doesn't do session affinity.
  sessionMode: "stateful"
  # "0" disables it.
  heartbeatInterval: "0"
  # trace, debug, info, warn, error, fatal, panic.
  logLevel: "info"
  # text or json.
  logFormat: "json"
  # in streamable-http mode this can only be set here. clients can't override it
  # via header or query param.
  tfeAddress: "https://app.terraform.io"
  # comma-separated. empty with strict mode rejects all cross-origin requests.
  allowedOrigins: ""
  # strict, development, or disabled.
  corsMode: "strict"
  # restrict tool calls to these orgs. empty allows any org the token can reach.
  allowedOrganizations: []
  # enables the destructive tools (delete_team, force_unlock_workspace, etc).
  enableTfOperations: false
  # where to send a browser that hits /. defaults to the terraform mcp docs page.
  redirectRootURL: ""
  # paths to a cert and key inside the container. mount them via volumes and
  # volumeMounts above.
  tls:
    certFile: ""
    keyFile: ""

otel:
  enabled: false
  metricsEndpoint: ""
  serviceName: "terraform-mcp-server"
